---
phase: 1.5-tests-exhaustifs
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: ["tests/conftest.py", "tests/test_angle_utils.py", "tests/test_config_loader.py", "pyproject.toml"]
autonomous: true
---

<objective>
## Goal
Mettre en place l'infrastructure pytest et créer des tests exhaustifs pour angle_utils et config_loader — les deux fondations utilisées par tous les autres modules.

## Purpose
Ces modules sont les plus réutilisés du projet. Les tester en premier garantit que la base est solide avant de tester les modules qui en dépendent.

## Output
- conftest.py avec fixtures partagées
- test_angle_utils.py — couverture complète des 6 fonctions
- test_config_loader.py — couverture complète du chargement config
- Configuration pytest dans pyproject.toml
</objective>

<acceptance_criteria>
## AC-1: Infrastructure pytest fonctionnelle
```gherkin
Given le projet DriftApp
When uv run pytest est exécuté
Then les tests s'exécutent sans erreur d'import
```

## AC-2: angle_utils couvert à 100%
```gherkin
Given les 6 fonctions de angle_utils.py
When les tests sont exécutés
Then chaque fonction est testée avec cas normaux, edge cases (0°/360°, négatifs, >360°) et limites
```

## AC-3: config_loader couvert exhaustivement
```gherkin
Given config_loader.py avec ses 11 dataclasses et ConfigLoader
When les tests sont exécutés
Then le chargement, le parsing de chaque section, les valeurs par défaut, et les erreurs sont testés
```
</acceptance_criteria>

<boundaries>
## DO NOT CHANGE
- Aucun fichier source ne doit être modifié
- data/config.json ne doit pas être modifié
- Le comportement de l'application ne change pas

## SCOPE LIMITS
- Tests uniquement pour angle_utils.py et config_loader.py
- Pas de modifications du code source
- Les tests documentent le comportement ACTUEL (y compris les bugs connus)
</boundaries>

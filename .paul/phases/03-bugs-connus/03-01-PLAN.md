---
phase: 03-bugs-connus
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/observatoire/catalogue.py
  - web/hardware/views.py
  - web/hardware/urls.py
  - tests/test_web_views.py
autonomous: true
---

<objective>
## Goal
Corriger les 2 bugs connus déferrés depuis la v5.1 : la méthode manquante `get_objets_disponibles()` dans GestionnaireCatalogue et les routes hardware manquantes (park, calibrate, end-session).

## Purpose
`ObjectListView` (GET /api/tracking/objects/) appelle `catalogue.get_objets_disponibles()` qui n'existe pas → erreur 500 en production. Les routes park/calibrate/end-session sont référencées dans la roadmap mais ne sont connectées à aucune vue.

## Output
- Méthode `get_objets_disponibles()` implémentée dans GestionnaireCatalogue
- Routes park, calibrate, end-session avec stubs retournant 501 Not Implemented
- Tests pour les 2 corrections
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Source Files
@core/observatoire/catalogue.py (GestionnaireCatalogue — pas de get_objets_disponibles)
@web/tracking/views.py (ObjectListView:100 appelle get_objets_disponibles)
@web/hardware/views.py (pas de ParkView, CalibrateView, EndSessionView)
@web/hardware/urls.py (6 routes, manquent park/calibrate/end-session)
@tests/test_web_views.py (tests existants)
</context>

<acceptance_criteria>

## AC-1: ObjectListView retourne la liste des objets
```gherkin
Given le catalogue contient des objets en cache
When GET /api/tracking/objects/ est appelé
Then la réponse est 200 avec count et objects[]
And chaque objet a au minimum un nom et des coordonnées
```

## AC-2: Routes park/calibrate/end-session répondent 501
```gherkin
Given les routes /api/hardware/park/, /api/hardware/calibrate/, /api/hardware/end-session/ existent
When POST est envoyé sur chacune
Then la réponse est 501 Not Implemented
And le body contient un message indiquant que la fonctionnalité est à venir
```

## AC-3: Tests couvrent les corrections
```gherkin
Given les tests existants dans tests/test_web_views.py
When uv run pytest tests/test_web_views.py -v
Then les tests existants passent toujours
And les nouveaux tests (objects list, stubs 501) passent
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Implémenter get_objets_disponibles() dans GestionnaireCatalogue</name>
  <files>core/observatoire/catalogue.py</files>
  <action>
    Ajouter la méthode `get_objets_disponibles()` à `GestionnaireCatalogue`.

    La méthode doit :
    - Lire le fichier cache local (`data/objets_cache.json` via `CACHE_FILE`)
    - Retourner une liste de dicts avec au minimum `name`, `ra_deg`, `dec_deg`
    - Retourner une liste vide si le cache n'existe pas ou est vide
    - Protéger par try/except (pas de crash si fichier corrompu)

    S'appuyer sur le pattern existant dans `rechercher_catalogue_local()` qui lit déjà le cache.
    Ne PAS ajouter de requête réseau — liste locale uniquement.
  </action>
  <verify>
    uv run python -c "from core.observatoire.catalogue import GestionnaireCatalogue; c = GestionnaireCatalogue(); print(len(c.get_objets_disponibles()), 'objets')"
  </verify>
  <done>AC-1 satisfait : méthode existe et retourne des objets depuis le cache</done>
</task>

<task type="auto">
  <name>Task 2: Ajouter les routes stub park/calibrate/end-session</name>
  <files>web/hardware/views.py, web/hardware/urls.py</files>
  <action>
    **Dans web/hardware/views.py**, ajouter 3 vues stub :

    ```python
    class ParkView(APIView):
        """POST /api/hardware/park/ — À implémenter."""
        def post(self, request):
            return Response(
                {'error': 'Fonction park non implémentée'},
                status=status.HTTP_501_NOT_IMPLEMENTED
            )

    class CalibrateView(APIView):
        """POST /api/hardware/calibrate/ — À implémenter."""
        def post(self, request):
            return Response(
                {'error': 'Fonction calibrate non implémentée'},
                status=status.HTTP_501_NOT_IMPLEMENTED
            )

    class EndSessionView(APIView):
        """POST /api/hardware/end-session/ — À implémenter."""
        def post(self, request):
            return Response(
                {'error': 'Fonction end-session non implémentée'},
                status=status.HTTP_501_NOT_IMPLEMENTED
            )
    ```

    **Dans web/hardware/urls.py**, ajouter les 3 routes :
    ```python
    path('park/', views.ParkView.as_view(), name='motor-park'),
    path('calibrate/', views.CalibrateView.as_view(), name='motor-calibrate'),
    path('end-session/', views.EndSessionView.as_view(), name='motor-end-session'),
    ```

    Éviter : implémenter la logique réelle (hors scope — sera dans un milestone dédié).
  </action>
  <verify>
    uv run pytest tests/test_web_views.py -v --tb=short (pas de régression)
  </verify>
  <done>AC-2 satisfait : routes existent et retournent 501</done>
</task>

<task type="auto">
  <name>Task 3: Tests pour les corrections</name>
  <files>tests/test_web_views.py</files>
  <action>
    Ajouter dans `tests/test_web_views.py` :

    **TestObjectListView** (compléter la classe existante si elle existe) :
    - `test_list_objects_returns_list` : GET /api/tracking/objects/ retourne 200 avec count et objects
    - Vérifier que le résultat est un dict avec 'count' et 'objects'

    **TestStubRoutes** (nouvelle classe) :
    - `test_park_returns_501` : POST /api/hardware/park/ → 501
    - `test_calibrate_returns_501` : POST /api/hardware/calibrate/ → 501
    - `test_end_session_returns_501` : POST /api/hardware/end-session/ → 501

    Utiliser les patterns de test existants (APIRequestFactory ou client Django).
  </action>
  <verify>uv run pytest tests/test_web_views.py -v</verify>
  <done>AC-3 satisfait : tous les tests passent</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- core/tracking/ (tracking stable après Phase 2)
- services/ (motor service stable après Phase 2)
- web/templates/ (UI hors scope)
- web/static/ (JS/CSS hors scope)

## SCOPE LIMITS
- get_objets_disponibles() : lecture cache locale uniquement, pas de requête SIMBAD
- Routes stub : retournent 501, pas d'implémentation réelle
- Pas de modification du frontend pour consommer les nouvelles routes

</boundaries>

<verification>
Before declaring plan complete:
- [ ] uv run pytest tests/test_web_views.py -v (tous les tests passent)
- [ ] uv run pytest tests/ -v (pas de régression — 771+ tests)
- [ ] uv run ruff check core/observatoire/catalogue.py web/hardware/views.py web/hardware/urls.py
- [ ] Tous les critères d'acceptation vérifiés
</verification>

<success_criteria>
- ObjectListView fonctionne (plus de crash 500)
- 3 routes stub retournent 501
- Tests couvrant les 2 corrections
- 0 régression
</success_criteria>

<output>
After completion, create `.paul/phases/03-bugs-connus/03-01-SUMMARY.md`
</output>

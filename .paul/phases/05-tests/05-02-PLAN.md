---
phase: 05-tests
plan: 02
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - tests/test_health_views.py
  - tests/test_session_views.py
  - tests/test_session_storage.py
autonomous: true
---

<objective>
## Goal
Étendre la couverture de tests aux modules web non testés : health/views.py, session/views.py et session/session_storage.py.

## Purpose
Compléter la couverture de la Phase 5 en testant les endpoints API critiques (diagnostic, sessions) qui n'ont aucun test, avant la Phase 6 (Validation).

## Output
- tests/test_health_views.py : tests pour les vues /api/health/
- tests/test_session_views.py : tests pour les vues /api/session/
- tests/test_session_storage.py : tests pour la couche de persistance sessions
- Suite totale : 720+ tests verts
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Prior Work
@.paul/phases/05-tests/05-01-SUMMARY.md
- 693 tests verts, 8 fichiers alignés
- ObjectListView.get_objets_disponibles() documenté comme bug production

## Source Files
@web/health/views.py — 5+ vues API health (health_check, motor_health, encoder_health, ipc_status, diagnostic)
@web/session/views.py — 5 vues API session (current, history, detail, save, delete)
@web/session/session_storage.py — Persistance sessions JSON (save, list, load, delete, cleanup)
@web/health/urls.py — Routes /api/health/
@web/session/urls.py — Routes /api/session/
</context>

<acceptance_criteria>

## AC-1: Health views testées
```gherkin
Given un fichier tests/test_health_views.py avec tests pour les vues health
When on exécute uv run pytest tests/test_health_views.py -v
Then au moins 10 tests passent couvrant health_check, motor_health, encoder_health, diagnostic
```

## AC-2: Session views testées
```gherkin
Given un fichier tests/test_session_views.py avec tests pour les vues session
When on exécute uv run pytest tests/test_session_views.py -v
Then au moins 8 tests passent couvrant current, history, detail, save, delete
```

## AC-3: Session storage testée
```gherkin
Given un fichier tests/test_session_storage.py avec tests pour la persistance
When on exécute uv run pytest tests/test_session_storage.py -v
Then au moins 8 tests passent couvrant save, list, load, delete, cleanup, ID generation
```

## AC-4: Suite complète sans régression
```gherkin
Given tous les nouveaux fichiers de test
When on exécute uv run pytest -v
Then 720+ tests passent, 0 erreurs, 0 échecs
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Créer tests pour health/views.py</name>
  <files>tests/test_health_views.py</files>
  <action>
    Créer tests/test_health_views.py avec :
    - Setup Django comme dans test_web_views.py (os.environ DJANGO_SETTINGS_MODULE)
    - Fixtures IPC mockées (status_file, encoder_file avec données valides/stales/manquantes)
    - Tests pour health_check() : healthy → 200, unhealthy → 503
    - Tests pour motor_health() : fichier frais → healthy, fichier stale → stale, fichier absent → error
    - Tests pour encoder_health() : idem motor
    - Tests pour diagnostic() : retourne bundle complet avec clés attendues
    - Tests pour _check_file_freshness() : frais/stale/manquant
    - Tests pour _read_ipc_file_content() : JSON valide/vide/corrompu/manquant
    - Mock subprocess pour check_update/apply_update (pas d'appels réels)

    Pattern : utiliser tmp_path pour les fichiers IPC, patcher les chemins via settings ou modules.
    Éviter : appels réels à git ou subprocess.
  </action>
  <verify>uv run pytest tests/test_health_views.py -v → 10+ tests verts</verify>
  <done>AC-1 satisfied : health views couvertes</done>
</task>

<task type="auto">
  <name>Task 2: Créer tests pour session/views.py et session_storage.py</name>
  <files>tests/test_session_views.py, tests/test_session_storage.py</files>
  <action>
    Créer tests/test_session_storage.py avec :
    - Fixture tmp_path pour le répertoire de sessions
    - Tests save_session() : génère ID, écrit JSON valide
    - Tests list_sessions() : triées par date, respecte limit, répertoire vide
    - Tests load_session() : existe → dict, manquant → None
    - Tests delete_session() : succès → True, manquant → False
    - Tests generate_session_id() : format YYYYMMDD_HHMMSS_SafeName
    - Tests _cleanup_old_sessions() : garde ≤ MAX_SESSIONS

    Créer tests/test_session_views.py avec :
    - Setup Django comme test_web_views.py
    - Mock motor_client.get_motor_status() pour simuler tracking actif/idle
    - Tests current_session() : tracking → 200, idle → 404
    - Tests session_history() : liste → 200, avec limit param
    - Tests session_detail() : existe → 200, manquant → 404
    - Tests save_session() : tracking → 200, idle → 400
    - Tests delete_session() : existe → 200, manquant → 404

    Pattern : mocker session_storage et motor_client, pas d'I/O réel dans les tests de vues.
    Éviter : dépendances entre tests de storage et tests de vues.
  </action>
  <verify>uv run pytest tests/test_session_storage.py tests/test_session_views.py -v → 16+ tests verts</verify>
  <done>AC-2 et AC-3 satisfied : session views et storage couverts</done>
</task>

<task type="auto">
  <name>Task 3: Valider la suite complète</name>
  <files>tests/</files>
  <action>
    Exécuter uv run pytest -v pour valider zéro régression.
    Lancer uv run ruff check sur les 3 nouveaux fichiers.
    Vérifier que le total atteint 720+ tests.
  </action>
  <verify>uv run pytest -v → 720+ passed, 0 failed, 0 errors</verify>
  <done>AC-4 satisfied : suite complète verte</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- core/ (code production stable)
- services/ (refactorisé Phase 4)
- web/ (vues, templates, static — pas de modification production)
- tests/ existants (693 tests stables depuis Plan 01)
- tests/conftest.py (fixtures partagées)

## SCOPE LIMITS
- Nouveaux tests uniquement — pas de correction de bugs production
- Pas de test pour logging_config.py ni encoder_reader.py (deprecated/infrastructure)
- Pas de test nécessitant hardware réel ou connexion réseau

</boundaries>

<verification>
Before declaring plan complete:
- [ ] uv run pytest tests/test_health_views.py -v → 10+ tests verts
- [ ] uv run pytest tests/test_session_storage.py -v → 8+ tests verts
- [ ] uv run pytest tests/test_session_views.py -v → 8+ tests verts
- [ ] uv run pytest -v → 720+ tests verts, 0 erreurs
- [ ] uv run ruff check tests/test_health_views.py tests/test_session_views.py tests/test_session_storage.py → clean
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- 3 nouveaux fichiers de tests créés
- 26+ nouveaux tests passent
- Suite complète 720+ tests verts
- Aucune modification du code production
- Code test propre (ruff check clean)
</success_criteria>

<output>
After completion, create `.paul/phases/05-tests/05-02-SUMMARY.md`
</output>

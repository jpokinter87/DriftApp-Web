---
phase: 05-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_moteur_simule.py
  - tests/test_tracker_unit.py
autonomous: true
---

<objective>
## Goal
Réparer les 2 fichiers de tests cassés par les refactorings des Phases 3-4 et valider que la suite complète (652+ tests) passe à 100%.

## Purpose
Débloquer 59 tests actuellement inaccessibles à cause d'erreurs d'import, permettant une baseline de tests fiable avant d'étendre la couverture (Plan 02).

## Output
- tests/test_moteur_simule.py : 33 tests débloqués
- tests/test_tracker_unit.py : 26 tests débloqués
- Suite complète : 652+ tests verts
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Prior Work
@.paul/phases/04-refactoring-services/04-01-SUMMARY.md
- Phase 4 a renommé `_simulated_position` → `_global_position` dans moteur_simule.py
- Phase 3 a réorganisé config_loader (DATA_DIR n'a jamais existé comme export)

## Source Files
@core/hardware/moteur_simule.py — variable globale renommée `_global_position`, fonctions `get_simulated_position()`, `set_simulated_position()`
@core/config/config_loader.py — exporte `load_config()`, pas de `DATA_DIR`
@core/observatoire/__init__.py — exporte `AstronomicalCalculations`, `PlanetaryEphemerides`
@tests/test_moteur_simule.py — import cassé ligne 19 (`_simulated_position`)
@tests/test_tracker_unit.py — import cassé ligne 22 (`DATA_DIR`)
</context>

<acceptance_criteria>

## AC-1: test_moteur_simule.py collecté et vert
```gherkin
Given le fichier tests/test_moteur_simule.py avec import corrigé
When on exécute uv run pytest tests/test_moteur_simule.py -v
Then les 33 tests sont collectés et passent tous
```

## AC-2: test_tracker_unit.py collecté et vert
```gherkin
Given le fichier tests/test_tracker_unit.py avec import corrigé
When on exécute uv run pytest tests/test_tracker_unit.py -v
Then les 26 tests sont collectés et passent tous
```

## AC-3: Suite complète sans régression
```gherkin
Given tous les fichiers de test corrigés
When on exécute uv run pytest -v
Then 652+ tests passent, 0 erreurs, 0 échecs
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Corriger les imports cassés dans test_moteur_simule.py</name>
  <files>tests/test_moteur_simule.py</files>
  <action>
    Ligne 15-20 : Supprimer `_simulated_position` de la liste d'imports.
    La variable globale a été renommée `_global_position` en Phase 4, mais les tests
    n'utilisent que `get_simulated_position()` et `set_simulated_position()` via les
    fonctions accesseurs — pas besoin d'importer la variable interne.

    Vérifier que le reste du fichier ne référence pas `_simulated_position` directement.
    Si oui, remplacer par `get_simulated_position()`.

    Éviter : Ne pas importer `_global_position` (variable interne, utiliser les accesseurs).
  </action>
  <verify>uv run pytest tests/test_moteur_simule.py -v → 33 tests passent</verify>
  <done>AC-1 satisfied : 33 tests collectés et verts</done>
</task>

<task type="auto">
  <name>Task 2: Corriger les imports cassés dans test_tracker_unit.py</name>
  <files>tests/test_tracker_unit.py</files>
  <action>
    Ligne 22 : Retirer `DATA_DIR` de l'import `from core.config.config_loader import load_config, DATA_DIR`.
    DATA_DIR n'existe pas dans config_loader.

    Chercher dans le fichier toutes les utilisations de `DATA_DIR` et les remplacer par
    `Path(__file__).resolve().parent.parent / "data"` (ou la constante appropriée déjà
    disponible via `load_config()`).

    Ligne 24 : Vérifier que `from core.observatoire import AstronomicalCalculations` fonctionne
    (l'__init__.py exporte bien cette classe).

    Éviter : Ne pas ajouter DATA_DIR à config_loader.py — garder la correction côté test.
  </action>
  <verify>uv run pytest tests/test_tracker_unit.py -v → 26 tests passent</verify>
  <done>AC-2 satisfied : 26 tests collectés et verts</done>
</task>

<task type="auto">
  <name>Task 3: Valider la suite complète et confirmer zéro régression</name>
  <files>tests/</files>
  <action>
    Exécuter la suite de tests complète avec uv run pytest -v.
    Vérifier que tous les tests passent (652+).
    Si des tests échouent pour des raisons autres que les imports corrigés,
    diagnostiquer et corriger (alignement API post-refactoring).

    Lancer aussi uv run ruff check tests/ pour vérifier la qualité du code test.
  </action>
  <verify>uv run pytest -v → 652+ passed, 0 failed, 0 errors</verify>
  <done>AC-3 satisfied : suite complète verte, pas de régression</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- core/ (tout le code production est stable après Phases 3-4)
- services/ (refactorisé et validé en Phase 4)
- web/ (templates, views, static)
- tests/conftest.py (fixtures partagées stables)
- Tous les autres fichiers tests qui passent déjà

## SCOPE LIMITS
- Ce plan ne couvre que la réparation des tests cassés
- Pas de nouveaux tests (couvert par Plan 02)
- Pas de modification du code production pour faire passer les tests

</boundaries>

<verification>
Before declaring plan complete:
- [ ] uv run pytest tests/test_moteur_simule.py -v → 33 tests verts
- [ ] uv run pytest tests/test_tracker_unit.py -v → 26 tests verts
- [ ] uv run pytest -v → 652+ tests verts, 0 erreurs
- [ ] uv run ruff check tests/test_moteur_simule.py tests/test_tracker_unit.py → clean
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- Les 2 fichiers cassés sont réparés et leurs tests passent
- La suite complète passe sans régression (652+ tests)
- Aucune modification du code production
- Code test propre (ruff check clean)
</success_criteria>

<output>
After completion, create `.paul/phases/05-tests/05-01-SUMMARY.md`
</output>

---
phase: 03-tests-complementaires
plan: 02
type: tdd
wave: 1
depends_on: ["03-01"]
files_modified:
  - tests/test_integration_ipc.py
  - tests/test_tracker_unit.py
autonomous: true
---

<objective>
## Goal
Ajouter des tests d'intégration IPC (motor_service en simulation reçoit/traite des commandes via fichiers) et augmenter la couverture de tracker.py (actuellement 14%).

## Purpose
Les tests unitaires actuels (413) couvrent bien les modules individuels mais pas les interactions entre composants. Le tracker.py à 14% est le module le plus critique et le moins couvert.

## Output
- test_integration_ipc.py : tests de bout en bout MotorService en mode simulation
- test_tracker_unit.py : tests unitaires du TrackingSession avec mocks
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/phases/03-tests-complementaires/03-01-SUMMARY.md

## Source Files
@services/motor_service.py (MotorService — IPC via /dev/shm/)
@core/tracking/tracker.py (TrackingSession — suivi d'objets)
</context>

<acceptance_criteria>

## AC-1: Tests d'intégration IPC
```gherkin
Given MotorService est initialisé en mode simulation
When une commande goto est écrite dans le fichier command
Then le service traite la commande et met à jour le fichier status
And la position simulée correspond à l'angle demandé
```

## AC-2: Tests tracker.py couverture augmentée
```gherkin
Given TrackingSession est instancié avec des mocks (moteur, calc, logger)
When les méthodes publiques sont testées (start_tracking, get_status, stop_tracking)
Then la couverture de tracker.py passe de 14% à >40%
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Tests d'intégration IPC (MotorService simulation)</name>
  <files>tests/test_integration_ipc.py</files>
  <action>
    Créer `tests/test_integration_ipc.py` avec des tests utilisant MotorService en mode simulation
    et des fichiers IPC temporaires (tmp_path) :

    1. **Fixture motor_service :**
       - Instancier MotorService (auto-détecte simulation)
       - Patcher COMMAND_FILE/STATUS_FILE/ENCODER_FILE vers tmp_path
       - Ne PAS lancer la boucle principale (pas de run())

    2. **Tests de commandes :**
       - test_goto_via_ipc : écrire une commande goto → appeler handle_command → vérifier status
       - test_jog_via_ipc : commande jog → position change
       - test_stop_via_ipc : commande stop → status idle
       - test_read_command : écrire JSON → read_command retourne les données
       - test_write_status : write_status → fichier JSON lisible

    3. **Tests d'initialisation :**
       - test_motor_service_init_simulation : vérifie mode simulation
       - test_parking_config_loaded : vérifie config parking

    Note: ces tests ne lancent PAS la boucle événementielle — ils appellent
    les méthodes directement pour tester le traitement des commandes.
  </action>
  <verify>`uv run pytest tests/test_integration_ipc.py -v`</verify>
  <done>AC-1 satisfait</done>
</task>

<task type="auto">
  <name>Task 2: Tests unitaires TrackingSession</name>
  <files>tests/test_tracker_unit.py</files>
  <action>
    Créer `tests/test_tracker_unit.py` avec des tests du TrackingSession mocké :

    1. **Fixture tracking_session :**
       - MoteurSimule comme moteur
       - AstronomicalCalculations réel (config site test)
       - TrackingLogger réel (tmp_path)
       - Abaque mocké ou réel si fichier disponible

    2. **Tests d'initialisation :**
       - test_construction : vérifie les attributs
       - test_encoder_disabled : encoder_config disabled → encoder_available=False

    3. **Tests get_status :**
       - test_get_status_idle : pas de tracking → status dict avec valeurs par défaut
       - test_get_status_keys : vérifie les clés du dict de status

    4. **Tests start/stop tracking :**
       - test_start_tracking_known_object : objet dans le cache → session démarre
       - test_stop_tracking : arrêt propre

    5. **Tests calcul position coupole :**
       - test_calculer_position_coupole : si abaque disponible → retourne angle

    Approche: tester les méthodes publiques, mocker les dépendances hardware.
  </action>
  <verify>`uv run pytest tests/test_tracker_unit.py -v`</verify>
  <done>AC-2 satisfait</done>
</task>

<task type="auto">
  <name>Task 3: Validation complète + couverture</name>
  <files>tests/</files>
  <action>
    1. Lancer la suite complète
    2. Vérifier la couverture tracker.py
    3. Corriger toute régression
  </action>
  <verify>
    - `uv run pytest tests/ -v --tb=short` → tous passent
    - `uv run pytest tests/ --cov=core/tracking/tracker --cov-report=term-missing` → >40%
  </verify>
  <done>Tous les AC satisfaits</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `services/motor_service.py` (on teste, on ne modifie pas)
- `core/tracking/tracker.py` (on teste, on ne modifie pas)
- Aucun fichier source — uniquement des tests

## SCOPE LIMITS
- Tests d'intégration sans lancer la boucle événementielle
- Pas de tests avec GPIO réel
- Pas de tests multi-processus (trop complexe pour ce plan)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/ -v` → tous passent
- [ ] Nombre total de tests > 420
- [ ] test_integration_ipc.py existe et passe
- [ ] test_tracker_unit.py existe et passe
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- Tests d'intégration IPC fonctionnels
- Couverture tracker.py améliorée
- > 420 tests passent, zéro régression
</success_criteria>

<output>
After completion, create `.paul/phases/03-tests-complementaires/03-02-SUMMARY.md`
</output>

---
phase: 02-refactoring-corrections
plan: 03
type: execute
wave: 1
depends_on: ["02-02"]
files_modified:
  - web/hardware/views.py
  - web/tracking/views.py
  - web/common/__init__.py
  - web/common/ipc_client.py
  - core/observatoire/calculations.py
  - core/observatoire/ephemerides.py
  - tests/test_web_views.py
  - tests/test_calculations.py
  - tests/test_ephemerides.py
autonomous: true
---

<objective>
## Goal
Éliminer la duplication de code : MotorServiceClient (C-06), fonctions Julian date (M-14), normalisation angles (M-15), et supprimer le code mort _add_time_component (M-16).

## Purpose
3 copies de la même logique = 3 endroits à maintenir. Réduire à une seule source de vérité pour chaque fonction. Supprimer le code mort qui encombre.

## Output
- MotorServiceClient extrait dans web/common/ipc_client.py
- _julian_date unique dans calculations.py, importée par ephemerides.py
- Normalisation angles importée depuis angle_utils dans calculations.py
- _add_time_component supprimé (code mort)
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/phases/02-refactoring-corrections/02-02-SUMMARY.md

## Source Files
@web/hardware/views.py (C-06 — MotorServiceClient)
@web/tracking/views.py (C-06 — MotorServiceClient dupliqué)
@core/observatoire/calculations.py (M-14, M-15, M-16)
@core/observatoire/ephemerides.py (M-14 — _julian_date)
@core/utils/angle_utils.py (M-15 — normalize_angle_180/360)
</context>

<acceptance_criteria>

## AC-1: MotorServiceClient unique (C-06)
```gherkin
Given MotorServiceClient est dupliqué dans hardware/views.py et tracking/views.py
When il est extrait dans web/common/ipc_client.py
Then les deux fichiers views importent depuis le même module
And la fonctionnalité IPC reste identique
```

## AC-2: Julian date unique (M-14)
```gherkin
Given _julian_date existe dans calculations.py et ephemerides.py
When ephemerides.py importe _calculate_julian_day depuis calculations.py
Then une seule implémentation reste
And les calculs astronomiques donnent les mêmes résultats
```

## AC-3: Normalisation angles sans duplication (M-15)
```gherkin
Given _normaliser_angle_180/360 dans calculations.py dupliquent angle_utils.py
When ils sont remplacés par des imports de angle_utils
Then calculations.py utilise les fonctions partagées
```

## AC-4: Code mort supprimé (M-16)
```gherkin
Given _add_time_component() n'est jamais appelée
When elle est supprimée de calculations.py
Then aucun test ne casse
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Extraire MotorServiceClient dans module partagé</name>
  <files>web/common/__init__.py, web/common/ipc_client.py, web/hardware/views.py, web/tracking/views.py, tests/test_web_views.py</files>
  <action>
    1. Créer `web/common/__init__.py` (fichier vide)
    2. Créer `web/common/ipc_client.py` avec la version complète de MotorServiceClient :
       - Fusionner les deux versions (hardware a `get_motor_status` + `get_encoder_status`, tracking a `get_status`)
       - Garder toutes les méthodes : send_command, get_motor_status, get_encoder_status
       - Instance globale : `motor_client = MotorServiceClient()`
    3. Dans `web/hardware/views.py` :
       - Supprimer la classe MotorServiceClient et l'instance
       - Ajouter : `from web.common.ipc_client import motor_client`
    4. Dans `web/tracking/views.py` :
       - Supprimer la classe MotorServiceClient et l'instance
       - Ajouter : `from web.common.ipc_client import motor_client`
       - Remplacer les appels `motor_client.get_status()` par `motor_client.get_motor_status()`
    5. Vérifier que les tests web_views passent toujours
  </action>
  <verify>`uv run pytest tests/test_web_views.py -v`</verify>
  <done>AC-1 satisfait</done>
</task>

<task type="auto">
  <name>Task 2: Dédupliquer Julian date, normalisation angles, supprimer code mort</name>
  <files>core/observatoire/calculations.py, core/observatoire/ephemerides.py, tests/test_calculations.py, tests/test_ephemerides.py</files>
  <action>
    1. **M-14 — Julian date :**
       Dans `ephemerides.py`, supprimer `_julian_date()` et importer depuis calculations :
       ```python
       from core.observatoire.calculations import CalculsAstronomiques
       ```
       Remplacer les appels `self._julian_date(dt)` par `CalculsAstronomiques._calculate_julian_day(dt)`

    2. **M-15 — Normalisation angles :**
       Dans `calculations.py`, remplacer les méthodes statiques dupliquées :
       - Supprimer `_normaliser_angle_180()` et `_normaliser_angle_360()`
       - Importer en haut du fichier :
         ```python
         from core.utils.angle_utils import normalize_angle_180, normalize_angle_360
         ```
       - Remplacer les appels internes `self._normaliser_angle_180(x)` par `normalize_angle_180(x)`
       - Remplacer `self._normaliser_angle_360(x)` par `normalize_angle_360(x)`

    3. **M-16 — Code mort :**
       Supprimer `_add_time_component()` de calculations.py (jamais appelée)

    4. Vérifier que les tests calculations et ephemerides passent
  </action>
  <verify>`uv run pytest tests/test_calculations.py tests/test_ephemerides.py -v`</verify>
  <done>AC-2, AC-3, AC-4 satisfaits</done>
</task>

<task type="auto">
  <name>Task 3: Validation complète</name>
  <files>tests/</files>
  <action>
    1. Lancer la suite complète
    2. Corriger toute régression
    3. Vérifier >= 416 tests
  </action>
  <verify>`uv run pytest tests/ -v --tb=short` → tous passent</verify>
  <done>Tous les AC satisfaits, zéro régression</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `core/utils/angle_utils.py` (source de vérité, pas de modification)
- `services/motor_service.py` (hors scope)
- `data/config.json`

## SCOPE LIMITS
- Uniquement les 4 findings listés (C-06, M-14, M-15, M-16)
- Pas de refactoring de l'architecture IPC
- Pas de modification fonctionnelle

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/ -v` → tous passent (>= 416)
- [ ] `grep -rn "class MotorServiceClient" web/` → 1 seul résultat (ipc_client.py)
- [ ] `grep -rn "_julian_date\|_calculate_julian_day" core/observatoire/` → 1 seul fichier (calculations.py)
- [ ] `grep -rn "_normaliser_angle" core/` → aucun résultat
- [ ] `grep -rn "_add_time_component" core/` → aucun résultat
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- MotorServiceClient unique dans web/common/ipc_client.py
- Julian date unique dans calculations.py
- Normalisation angles importée depuis angle_utils
- Code mort _add_time_component supprimé
- >= 416 tests passent, zéro régression
</success_criteria>

<output>
After completion, create `.paul/phases/02-refactoring-corrections/02-03-SUMMARY.md`
</output>

---
phase: 02-refactoring-corrections
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - core/hardware/moteur.py
  - core/observatoire/catalogue.py
  - web/tracking/views.py
  - tests/test_daemon_encoder_reader.py
  - tests/test_catalogue.py
  - tests/test_web_views.py
autonomous: true
---

<objective>
## Goal
Corriger 4 bugs latents identifiés en Phase 1 : read_angle() boucle infinie (H-04), read_stable() moyenne incorrecte 0°/360° (H-01), résultat planète incohérent (H-14), et ObjectListView crashe (M-27).

## Purpose
Ces bugs sont des bombes à retardement en production : H-04 peut geler le motor_service, H-01 donne une position erronée au passage 0°/360°, H-14 empêche le tracking des planètes, M-27 crashe l'API web.

## Output
- 4 bugs corrigés dans le code production
- Tests existants adaptés + nouveaux tests pour chaque correction
- Zéro régression
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/phases/02-refactoring-corrections/02-01-SUMMARY.md

## Source Files
@core/hardware/moteur.py (H-01, H-04 — DaemonEncoderReader)
@core/observatoire/catalogue.py (H-14 — rechercher planètes)
@web/tracking/views.py (M-27 — ObjectListView)

## Review Reports
@.paul/phases/01-code-review-audit/01-01-REVIEW.md (H-01, H-04)
@.paul/phases/01-code-review-audit/01-02-REVIEW.md (H-14)
@.paul/phases/01-code-review-audit/01-03-REVIEW.md (M-27)
</context>

<acceptance_criteria>

## AC-1: read_angle() timeout sur statut inconnu (H-04)
```gherkin
Given le démon encodeur retourne un statut inconnu (ex: "CALIBRATING", "ERROR")
When read_angle() est appelé avec timeout_ms=200
Then la fonction retourne l'angle après timeout au lieu de boucler indéfiniment
```

## AC-2: read_stable() moyenne circulaire correcte (H-01)
```gherkin
Given des lectures proches de 0°/360° (ex: [359.5, 0.2, 0.5])
When read_stable() calcule la moyenne
Then le résultat est ~0.07° (moyenne circulaire) et non ~120° (moyenne arithmétique)
```

## AC-3: Résultat planète normalisé (H-14)
```gherkin
Given une recherche de planète (ex: "Jupiter") réussit
When le résultat est retourné par rechercher()
Then il contient "nom" (pas "name"), "is_planet": True, et les mêmes clés qu'un objet SIMBAD
```

## AC-4: ObjectListView ne crashe pas (M-27)
```gherkin
Given l'API /api/tracking/objects/ est appelée
When GestionnaireCatalogue n'a pas de méthode get_objets_disponibles()
Then l'API retourne la liste des objets du catalogue sans crash
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Corriger read_angle() et read_stable() dans DaemonEncoderReader</name>
  <files>core/hardware/moteur.py, tests/test_daemon_encoder_reader.py</files>
  <action>
    1. **H-04 — read_angle() boucle infinie :**
       Dans `DaemonEncoderReader.read_angle()` (ligne ~82), ajouter un check timeout APRÈS le test de statut.
       Actuellement le timeout ne se déclenche que quand `data is None`. Ajouter :
       ```python
       # Après le bloc elif status.startswith("SPI"):
       else:
           # Statut inconnu — timeout au lieu de boucle infinie
           if elapsed_ms > timeout_ms:
               self.logger.warning(f"Démon encodeur statut inconnu: {status}, retour angle={angle}")
               return angle
           time.sleep(0.01)
       ```

    2. **H-01 — read_stable() moyenne circulaire :**
       Remplacer `sum(positions) / len(positions)` par une moyenne circulaire :
       ```python
       import math
       sin_sum = sum(math.sin(math.radians(p)) for p in positions)
       cos_sum = sum(math.cos(math.radians(p)) for p in positions)
       mean_angle = math.degrees(math.atan2(sin_sum, cos_sum)) % 360.0
       return mean_angle
       ```

    3. **Tests :** Ajouter dans test_daemon_encoder_reader.py :
       - Test read_angle timeout sur statut inconnu (mock retournant {"angle": 45.0, "status": "CALIBRATING"})
       - Test read_stable moyenne circulaire (mock retournant [359.5, 0.2, 0.5] → ~0.07°)
       - Test read_stable moyenne normale (mock retournant [10.0, 11.0, 12.0] → 11.0°)
  </action>
  <verify>
    - `uv run pytest tests/test_daemon_encoder_reader.py -v`
    - Vérifier que les nouveaux tests passent
    - Vérifier que les tests existants ne cassent pas
  </verify>
  <done>AC-1 et AC-2 satisfaits</done>
</task>

<task type="auto">
  <name>Task 2: Normaliser résultat planète et corriger ObjectListView</name>
  <files>core/observatoire/catalogue.py, web/tracking/views.py, tests/test_catalogue.py, tests/test_web_views.py</files>
  <action>
    1. **H-14 — Résultat planète incohérent :**
       Dans `catalogue.py:rechercher()`, modifier le dict retour des planètes (ligne ~280) :
       ```python
       return {
           "nom": identifiant.capitalize(),  # "nom" au lieu de "name"
           "ra_deg": ra_deg,
           "dec_deg": dec_deg,
           "type": "planet",
           "is_planet": True,
           "source": "ephemerides",
       }
       ```

    2. **M-27 — ObjectListView crashe :**
       Dans `web/tracking/views.py:148`, remplacer :
       ```python
       objects = catalogue.get_objets_disponibles()
       ```
       par :
       ```python
       objects = catalogue.objets
       ```
       (La méthode `get_objets_disponibles()` n'existe pas, `catalogue.objets` est le dict des objets.)

    3. **Tests catalogue :** Mettre à jour `test_planet_result_structure_bug` pour vérifier la correction :
       - "nom" au lieu de "name"
       - "is_planet" présent et True

    4. **Tests web :** Ajouter ou corriger le test pour ObjectListView si nécessaire.
  </action>
  <verify>
    - `uv run pytest tests/test_catalogue.py tests/test_web_views.py -v`
  </verify>
  <done>AC-3 et AC-4 satisfaits</done>
</task>

<task type="auto">
  <name>Task 3: Validation complète</name>
  <files>tests/</files>
  <action>
    1. Lancer la suite complète de tests
    2. Corriger toute régression
    3. Vérifier que le nombre de tests a augmenté (>= 411 + nouveaux)
  </action>
  <verify>
    - `uv run pytest tests/ -v --tb=short` → tous passent
    - Nombre de tests > 411
  </verify>
  <done>Tous les AC satisfaits, zéro régression</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `data/config.json` (pas de modification de configuration)
- `services/motor_service.py` (hors scope)
- `core/tracking/tracker.py` (hors scope — H-09 sera un plan séparé)
- `core/hardware/feedback_controller.py` (H-02 sera un plan séparé)
- `ems22d_calibrated.py` (M-33 sera un plan séparé — daemon sur Pi)

## SCOPE LIMITS
- Uniquement les 4 bugs listés (H-01, H-04, H-14, M-27)
- Pas de refactoring structural
- Pas de modification de l'architecture IPC

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/ -v` → tous passent
- [ ] read_angle() timeout sur statut inconnu vérifié par test
- [ ] read_stable() moyenne circulaire vérifié par test
- [ ] Résultat planète contient "nom" et "is_planet"
- [ ] ObjectListView fonctionne sans crash
- [ ] Nombre de tests > 411
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- 4 bugs corrigés (H-01, H-04, H-14, M-27)
- Nouveaux tests couvrant chaque correction
- Suite complète > 411 tests, tous passent
- Zéro régression
</success_criteria>

<output>
After completion, create `.paul/phases/02-refactoring-corrections/02-02-SUMMARY.md`
</output>

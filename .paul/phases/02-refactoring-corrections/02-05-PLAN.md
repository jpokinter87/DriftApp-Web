---
phase: 02-refactoring-corrections
plan: 05
type: execute
wave: 1
depends_on: ["02-04"]
files_modified:
  - core/tracking/adaptive_tracking.py
  - main.py
  - main_gui.py
  - tests/test_adaptive_tracking.py
autonomous: true
---

<objective>
## Goal
Simplifier verify_shortest_path() en utilisant shortest_angular_distance() (H-11) et extraire le bootstrap commun de main.py/main_gui.py (H-20).

## Purpose
H-11 : 60 lignes de calcul angulaire dupliquent une fonction existante de 10 lignes — risque de divergence.
H-20 : 90% de duplication entre les deux points d'entrée — maintenance double pour tout changement.

## Output
- verify_shortest_path() simplifié (~10 lignes vs ~60)
- Bootstrap commun extrait, main.py et main_gui.py réduits à ~10 lignes chacun
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/phases/02-refactoring-corrections/02-04-SUMMARY.md

## Source Files
@core/tracking/adaptive_tracking.py (H-11 — verify_shortest_path)
@core/utils/angle_utils.py (shortest_angular_distance)
@main.py (H-20 — duplication)
@main_gui.py (H-20 — duplication)
</context>

<acceptance_criteria>

## AC-1: verify_shortest_path utilise shortest_angular_distance (H-11)
```gherkin
Given verify_shortest_path() réimplémente le calcul du chemin le plus court
When il est simplifié pour utiliser shortest_angular_distance()
Then les résultats sont identiques (mêmes delta, mêmes descriptions)
And tous les tests existants passent sans modification
```

## AC-2: Bootstrap commun extrait (H-20)
```gherkin
Given main.py et main_gui.py sont identiques à 90%
When une fonction bootstrap() commune est extraite
Then main.py appelle bootstrap(DriftApp, "SUIVI COUPOLE", "DEBUG")
And main_gui.py appelle bootstrap(DriftAppGUI, "SUIVI COUPOLE (GUI)", "INFO")
And le comportement est identique à l'original
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Simplifier verify_shortest_path</name>
  <files>core/tracking/adaptive_tracking.py, tests/test_adaptive_tracking.py</files>
  <action>
    1. Importer shortest_angular_distance en haut du fichier :
       ```python
       from core.utils.angle_utils import shortest_angular_distance
       ```

    2. Réécrire `verify_shortest_path()` en utilisant `shortest_angular_distance()` :
       ```python
       def verify_shortest_path(self, current_position: float, target_position: float) -> Tuple[float, str]:
           delta = shortest_angular_distance(current_position, target_position)

           abs_delta = abs(delta)
           direction = "horaire" if delta >= 0 else "anti-horaire"
           description = f"Chemin le plus court: {direction} ({abs_delta:.1f}°)"

           if abs_delta > 30:
               self.logger.info(f"🔍 Vérification chemin:")
               self.logger.info(f"   Position actuelle: {current_position % 360:.1f}°")
               self.logger.info(f"   Position coupole: {target_position % 360:.1f}°")
               self.logger.info(f"   ✓ Choisi: {direction} ({abs_delta:.1f}°)")

           return delta, description
       ```

    3. NE PAS modifier les tests — ils doivent passer tels quels pour prouver l'équivalence
  </action>
  <verify>`uv run pytest tests/test_adaptive_tracking.py -v` — tous les tests existants passent</verify>
  <done>AC-1 satisfait</done>
</task>

<task type="auto">
  <name>Task 2: Extraire bootstrap commun</name>
  <files>main.py, main_gui.py</files>
  <action>
    1. Dans `main.py`, extraire une fonction `bootstrap(app_class, title, log_level)` :
       ```python
       def bootstrap(app_class, title: str = "SUIVI COUPOLE", log_level: str = "DEBUG"):
           """Bootstrap commun pour les points d'entrée DriftApp."""
           # 1. Logging
           try:
               log_file = setup_logging(log_dir="logs", log_level=log_level,
                                        max_bytes=10*1024*1024, backup_count=5)
               print(f"📝 Logging configuré : {log_file}")
           except Exception as e:
               print(f"⚠️  Erreur configuration logging : {e}")

           # 2. Banner
           print("\n" + "=" * 60)
           print(f"OBSERVATOIRE - {title}")
           print("=" * 60)

           # 3. Config
           try:
               config = load_config()
               print(f"✅ Configuration chargée : {config.site.nom}")
           except Exception as e:
               print(f"❌ Erreur chargement config : {e}")
               return

           # 4. Hardware
           is_prod, hw_info = HardwareDetector.detect_hardware()
           if is_prod:
               print("✓ PRODUCTION")
               print(f"  {hw_info.get('rpi_model', 'RPi')}")
           else:
               print("⚠ SIMULATION")
               print(f"  {hw_info['system']}/{hw_info['machine']}")
           print("\n" + "=" * 60 + "\n")

           # 5. System info
           try:
               log_system_info()
           except Exception as e:
               print(f"⚠️  Erreur log système : {e}")

           # 6. Run
           try:
               app = app_class(config)
               app.run()
           except KeyboardInterrupt:
               print("\n\n⏸️  Application interrompue par l'utilisateur")
           except Exception as e:
               print(f"\n❌ Erreur fatale : {e}")
               import traceback
               traceback.print_exc()
           finally:
               try:
                   close_logging()
                   print("\n✅ Logging fermé proprement")
               except Exception as e:
                   print(f"\n⚠️  Erreur fermeture logging : {e}")
       ```

    2. Simplifier `main.py:main()` :
       ```python
       def main():
           bootstrap(DriftApp, "SUIVI COUPOLE", "DEBUG")
       ```

    3. Simplifier `main_gui.py` de la même façon :
       - Importer bootstrap depuis main.py
       - `main()` appelle `bootstrap(DriftAppGUI, "SUIVI COUPOLE (GUI)", "INFO")`

    4. Supprimer `time.sleep(0.5)` de main.py (finding L-22 — inutile)
  </action>
  <verify>
    - `uv run python -c "from main import bootstrap; print('OK')"` → import réussi
    - `uv run python -c "from main_gui import main; print('OK')"` → import réussi
  </verify>
  <done>AC-2 satisfait</done>
</task>

<task type="auto">
  <name>Task 3: Validation complète</name>
  <files>tests/</files>
  <action>
    1. Lancer la suite complète
    2. Corriger toute régression
  </action>
  <verify>`uv run pytest tests/ -v --tb=short` → tous passent (>= 412)</verify>
  <done>Tous les AC satisfaits, zéro régression</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `core/utils/angle_utils.py` (source de vérité)
- `core/tracking/tracker.py` (consommateur de verify_shortest_path, ne pas modifier)
- `services/motor_service.py`
- Tests existants de verify_shortest_path (preuve d'équivalence)

## SCOPE LIMITS
- H-02 et H-09 reportés à Phase 3
- Pas de modification fonctionnelle
- Le bootstrap supprime time.sleep(0.5) (L-22) mais ne change rien d'autre

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/ -v` → tous passent (>= 412)
- [ ] verify_shortest_path utilise shortest_angular_distance
- [ ] main.py et main_gui.py partagent bootstrap()
- [ ] Pas de duplication de calcul angulaire
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- verify_shortest_path simplifié (60 → ~10 lignes)
- Bootstrap commun, main/main_gui réduits
- >= 412 tests passent, zéro régression
</success_criteria>

<output>
After completion, create `.paul/phases/02-refactoring-corrections/02-05-SUMMARY.md`
</output>

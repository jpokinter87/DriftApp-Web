---
phase: 03-refactoring-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/config/config.py
  - core/config/config_loader.py
  - core/hardware/moteur.py
  - core/hardware/daemon_encoder_reader.py
  - core/hardware/acceleration_ramp.py
  - core/hardware/moteur_simule.py
  - core/tracking/tracking_corrections_mixin.py
autonomous: true
---

<objective>
## Goal
Corriger les 7 issues Critical et les issues High les plus impactantes dans core/.

## Purpose
Éliminer les bugs qui peuvent causer des dysfonctionnements en production : AttributeError, erreurs de calcul d'angle, race conditions, exceptions masquées.

## Output
- 7 issues Critical corrigées
- Issues High prioritaires corrigées
- Tests existants toujours verts (407+)
</objective>

<context>
## Project Context
@.paul/PROJECT.md

## Audit Results
@.paul/phases/02-audit-code/02-01-REVIEW.md

## Source Files
@core/config/config.py
@core/hardware/moteur.py
@core/hardware/daemon_encoder_reader.py
@core/hardware/acceleration_ramp.py
@core/hardware/moteur_simule.py
@core/tracking/tracking_corrections_mixin.py
@core/utils/angle_utils.py
</context>

<acceptance_criteria>

## AC-1: Issues Critical corrigées
```gherkin
Given les 5 issues Critical identifiées dans l'audit core/
When les corrections sont appliquées
Then C-20 (log_to_web), C-10 (moyenne angles), C-11 (race condition JSON),
     C-07 (bare except), C-01 (chemins relatifs) sont corrigées
```

## AC-2: Issues High prioritaires corrigées
```gherkin
Given les issues High les plus impactantes
When les corrections sont appliquées
Then C-02 (config silencieuse), C-08 (bare except nettoyer),
     C-09 (import lgpio répété), C-13 (stop_requested simulation),
     C-16 (division par zéro ramp) sont corrigées
```

## AC-3: Pas de régression
```gherkin
Given les corrections appliquées
When pytest est exécuté
Then les 407+ tests existants passent toujours
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Corriger les 5 issues Critical</name>
  <files>core/tracking/tracking_corrections_mixin.py, core/hardware/daemon_encoder_reader.py, core/hardware/moteur.py, core/config/config.py, core/config/config_loader.py</files>
  <action>
    **[C-20] log_to_web() manquant** (tracking_corrections_mixin.py:181)
    - Remplacer `self.log_to_web("Mode degrade...", "warning")` par `self.logger.warning("Mode degrade...")`
    - Vérifier qu'il n'y a pas d'autres appels à log_to_web dans les mixins

    **[C-10] Moyenne arithmétique d'angles** (daemon_encoder_reader.py:189)
    - Remplacer `sum(positions) / len(positions)` par une moyenne circulaire :
      ```python
      import math
      sin_sum = sum(math.sin(math.radians(p)) for p in positions)
      cos_sum = sum(math.cos(math.radians(p)) for p in positions)
      mean_angle = math.degrees(math.atan2(sin_sum, cos_sum)) % 360
      ```

    **[C-11] Race condition lecture JSON** (daemon_encoder_reader.py:67-70)
    - Ajouter un verrou fcntl LOCK_SH autour de la lecture dans read_raw() :
      ```python
      import fcntl
      with open(self.shm_file, 'r') as f:
          fcntl.flock(f, fcntl.LOCK_SH)
          data = json.loads(f.read())
          fcntl.flock(f, fcntl.LOCK_UN)
      ```

    **[C-07] bare except dans _init_gpio** (moteur.py:211)
    - Remplacer `except:` par `except (OSError, Exception):`
    - Idem pour C-08: remplacer `except: pass` dans nettoyer() (lignes 531, 536)
      par `except Exception: pass`

    **[C-01] Chemins relatifs** (config.py:17-20)
    - Remplacer `DATA_DIR = Path("data")` par
      `DATA_DIR = Path(__file__).resolve().parent.parent.parent / "data"`
    - Vérifier config_loader.py pour le même pattern
  </action>
  <verify>
    uv run pytest tests/ --ignore=tests/test_moteur_simule.py --ignore=tests/test_exceptions.py --ignore=tests/test_update_checker.py --ignore=tests/test_web_views.py --ignore=tests/test_motor_service.py --ignore=tests/test_integration_ipc.py --ignore=tests/test_tracker_unit.py --ignore=tests/test_tracking_logger.py --ignore=tests/test_hardware_detector.py --ignore=tests/test_config_loader.py --ignore=tests/test_catalogue.py --ignore=tests/test_ephemerides.py --tb=short
  </verify>
  <done>AC-1 satisfait : 5 issues Critical corrigées, tests verts</done>
</task>

<task type="auto">
  <name>Task 2: Corriger les issues High prioritaires</name>
  <files>core/config/config.py, core/hardware/moteur.py, core/hardware/acceleration_ramp.py, core/hardware/moteur_simule.py, core/hardware/daemon_encoder_reader.py</files>
  <action>
    **[C-02] Config silencieuse** (config.py:55,67)
    - Dans _load_json, ajouter un logging.warning quand une exception est capturée
    - Restreindre à `except (json.JSONDecodeError, FileNotFoundError, OSError):`

    **[C-09] import lgpio dans faire_un_pas** (moteur.py:337)
    - Stocker la référence `self._gpio_write = lgpio.gpio_write` dans _init_gpio()
    - Utiliser `self._gpio_write(...)` dans faire_un_pas() au lieu de `lgpio.gpio_write(...)`

    **[C-13] MoteurSimule stop_requested** (moteur_simule.py:164-195)
    - Découper le sleep dans rotation() en petits intervalles (0.05s)
    - Vérifier stop_requested entre chaque intervalle

    **[C-14] Singleton non thread-safe** (daemon_encoder_reader.py:200-213)
    - Ajouter threading.Lock pour protéger get_daemon_reader()

    **[C-16] Division par zéro ramp** (acceleration_ramp.py:177)
    - Ajouter garde `if self.accel_end == 0: return self.target_delay`
  </action>
  <verify>
    uv run pytest tests/ --ignore=tests/test_moteur_simule.py --ignore=tests/test_exceptions.py --ignore=tests/test_update_checker.py --ignore=tests/test_web_views.py --ignore=tests/test_motor_service.py --ignore=tests/test_integration_ipc.py --ignore=tests/test_tracker_unit.py --ignore=tests/test_tracking_logger.py --ignore=tests/test_hardware_detector.py --ignore=tests/test_config_loader.py --ignore=tests/test_catalogue.py --ignore=tests/test_ephemerides.py --tb=short
  </verify>
  <done>AC-2 satisfait : issues High corrigées, tests verts</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- web/templates/*.html, web/static/*
- services/ (Phase 4)
- core/tracking/tracker.py (issues Medium/Low seulement — pas dans ce plan)
- core/observatoire/ (issues Medium/Low — pas dans ce plan)
- Architecture des mixins (ne pas consolider — hors scope)

## SCOPE LIMITS
- Critical et High uniquement — Medium/Low dans Plan 03-02
- Corrections minimales et ciblées
- Pas de refactoring architectural

</boundaries>

<verification>
Avant de déclarer le plan complet :
- [ ] C-20, C-10, C-11, C-07, C-01 corrigées
- [ ] C-02, C-08, C-09, C-13, C-14, C-16 corrigées
- [ ] 407+ tests passent
- [ ] Aucun fichier hors scope modifié
</verification>

<success_criteria>
- 5 issues Critical + 6 issues High corrigées
- Tests existants verts
- Code prêt pour les corrections Medium/Low (Plan 03-02)
</success_criteria>

<output>
After completion, create `.paul/phases/03-refactoring-core/03-01-SUMMARY.md`
</output>

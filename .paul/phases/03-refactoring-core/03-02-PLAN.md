---
phase: 03-refactoring-core
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - core/hardware/moteur.py
  - core/tracking/adaptive_tracking.py
  - core/tracking/tracking_state_mixin.py
  - core/tracking/tracker.py
  - core/observatoire/calculations.py
  - core/observatoire/catalogue.py
  - core/observatoire/ephemerides.py
  - core/utils/angle_utils.py
autonomous: true
---

<objective>
## Goal
Corriger les issues Medium et Low restantes dans core/ : code mort, duplication, qualité.

## Purpose
Améliorer la maintenabilité et réduire les risques de bugs futurs. Éliminer le code mort et la duplication.

## Output
- Issues Medium et Low corrigées
- Code mort supprimé
- Duplication réduite (centralisation angle_utils)
</objective>

<context>
## Audit Results
@.paul/phases/02-audit-code/02-01-REVIEW.md

## Prior Work
@.paul/phases/03-refactoring-core/03-01-SUMMARY.md
</context>

<acceptance_criteria>

## AC-1: Code mort supprimé
```gherkin
Given les méthodes mortes identifiées dans l'audit
When les corrections sont appliquées
Then _valider_config, _init_parametres_rampe, _calculer_delai_rampe sont supprimées
And la variable globale gpio_handle est supprimée
And to_dict() NotImplementedError est supprimé
```

## AC-2: Duplication d'angles centralisée
```gherkin
Given la logique de normalisation dupliquée dans 5+ fichiers
When la centralisation est faite
Then les fichiers utilisent angle_utils au lieu de réimplémenter
```

## AC-3: Issues Medium observatoire corrigées
```gherkin
Given les issues Medium dans observatoire/
When les corrections sont appliquées
Then C-29 (config relue), C-30 (code mort), C-33 (exceptions avalées) sont corrigées
```

## AC-4: Pas de régression
```gherkin
Given toutes les corrections
When pytest est exécuté
Then les 407+ tests passent
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Supprimer le code mort et corriger les issues Medium/Low</name>
  <files>core/hardware/moteur.py, core/config/config_loader.py, core/tracking/adaptive_tracking.py, core/tracking/tracking_state_mixin.py, core/tracking/tracker.py, core/observatoire/calculations.py, core/observatoire/catalogue.py, core/observatoire/ephemerides.py, core/utils/angle_utils.py</files>
  <action>
    **Code mort (Low) :**
    - [C-17] Supprimer _valider_config() et son appel dans moteur.py
    - [C-18] Supprimer _init_parametres_rampe() et son appel dans moteur.py
    - [C-19] Supprimer _calculer_delai_rampe() dans moteur.py
    - [C-15] Supprimer la variable globale gpio_handle dans moteur.py
    - [C-05] Supprimer to_dict() dans config_loader.py ou l'implémenter
    - [C-30] Supprimer le `if angle < 0` mort dans calculations.py

    **Duplication (Medium/Low) :**
    - [C-37] Centraliser la normalisation d'angle :
      - calculations.py : remplacer _normaliser_angle_360 par angle_utils.normalize_angle_360
      - adaptive_tracking.py : utiliser shortest_angular_distance de angle_utils au lieu de verify_shortest_path (garder le logging en wrapper)
    - [C-25] Simplifier verify_shortest_path pour déléguer le calcul à angle_utils
    - [C-36] Remplacer les boucles while par modulo dans angle_utils.shortest_angular_distance

    **Qualité (Medium) :**
    - [C-22] tracker.py : instancier PlanetaryEphemerides une seule fois dans __init__
    - [C-26] tracking_state_mixin.py : séparer les métadonnées des compteurs mode_time
    - [C-27] tracker.py : documenter le couplage inverse web.session (ne pas corriger l'architecture, juste ajouter un commentaire)
    - [C-29] catalogue.py : utiliser SITE_LATITUDE/SITE_LONGITUDE de config au lieu de relire config.json
    - [C-33] ephemerides.py : logger l'exception avant de retourner None
    - [C-34] catalogue.py : utiliser self.simbad au lieu de recréer une instance
    - [C-31] calculations.py : protéger contre heures >= 24 avec modulo

    **Ne PAS toucher :**
    - [C-04] save_config() — documentation suffit (Medium, faible impact)
    - [C-06] Duplication config.py/config_loader.py — refactoring trop large pour ce plan
    - [C-24] Zones critiques multiples — documentation suffit
    - [C-28] deja_jnow — documentation suffit
  </action>
  <verify>
    uv run pytest tests/ --ignore=tests/test_moteur_simule.py --ignore=tests/test_exceptions.py --ignore=tests/test_update_checker.py --ignore=tests/test_web_views.py --ignore=tests/test_motor_service.py --ignore=tests/test_integration_ipc.py --ignore=tests/test_tracker_unit.py --ignore=tests/test_tracking_logger.py --ignore=tests/test_hardware_detector.py --ignore=tests/test_config_loader.py --ignore=tests/test_catalogue.py --ignore=tests/test_ephemerides.py --tb=short
  </verify>
  <done>AC-1, AC-2, AC-3, AC-4 satisfaits</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- web/templates/*.html, web/static/*
- services/ (Phase 4)
- Architecture des mixins (ne pas consolider)
- core/config/config.py structure globale (duplication config.py/config_loader.py hors scope)

## SCOPE LIMITS
- Medium et Low uniquement
- Pas de changements architecturaux
- Pas de nouveaux fichiers
- Si un test casse à cause d'une suppression de code mort, adapter le test

</boundaries>

<verification>
Avant de déclarer le plan complet :
- [ ] Code mort supprimé (5+ méthodes/variables)
- [ ] Duplication angle réduite
- [ ] Issues Medium observatoire corrigées
- [ ] 407+ tests passent
</verification>

<success_criteria>
- ~20 issues Medium/Low corrigées
- Code plus propre et maintenable
- Tests verts
</success_criteria>

<output>
After completion, create `.paul/phases/03-refactoring-core/03-02-SUMMARY.md`
</output>

---
phase: 06-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/test_cross_layer.py]
autonomous: true
---

<objective>
## Goal
Valider l'integration cross-couche du systeme (Django views -> IPC -> MotorService -> handlers -> retour status) et confirmer la suite complete de 738+ tests sans regression.

## Purpose
Cloturer le milestone v5.1 avec une validation fonctionnelle complete. Les phases 1-5 ont synchronise, audite, refactore et teste chaque couche individuellement. Cette phase verifie que les couches fonctionnent ensemble.

## Output
- `tests/test_cross_layer.py` : tests d'integration cross-couche
- Suite complete verte (738+ tests, 0 echecs)
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Prior Work
@.paul/phases/05-tests/05-02-SUMMARY.md

## Source Files
@tests/test_web_views.py
@tests/test_integration_ipc.py
@tests/test_integration.py
@tests/test_e2e.py
@web/common/ipc_client.py
@services/motor_service.py
@services/ipc_manager.py
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /code-review | required | Apres phase completee, avant UNIFY | ○ |

**BLOCKING:** /code-review doit etre invoque apres execution, avant UNIFY.

## Skill Invocation Checklist
- [ ] /code-review loaded (apres execution des taches)
</skills>

<acceptance_criteria>

## AC-1: Tests cross-couche Django -> IPC -> MotorService
```gherkin
Given un MotorService initialise en mode simulation avec fichiers IPC temporaires
When Django envoie une commande GOTO via MotorServiceClient
Then MotorService la recoit, l'execute, et le status lu par Django reflete la position finale
```

## AC-2: Tests cross-couche flux STOP
```gherkin
Given un MotorService en mode simulation
When Django envoie une commande STOP via MotorServiceClient
Then le status revient a idle et est lisible cote Django
```

## AC-3: Suite complete sans regression
```gherkin
Given la suite de tests existante (738+ tests)
When on execute `uv run pytest -v`
Then tous les tests passent (0 echecs, 0 erreurs)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Creer tests d'integration cross-couche</name>
  <files>tests/test_cross_layer.py</files>
  <action>
    Creer un module de tests qui exerce le flux complet sur une seule machine :

    1. Setup : instancier MotorService avec fichiers IPC dans tmp_path (meme pattern que test_integration_ipc.py)
    2. Instancier MotorServiceClient pointe vers les memes fichiers IPC
    3. Tests :
       - test_django_goto_to_motor_service : MotorServiceClient.send_command("goto", angle=90) -> MotorService.process_command lit la commande -> execute -> _write_status -> MotorServiceClient.get_motor_status() verifie position ~90
       - test_django_jog_to_motor_service : meme flux avec commande JOG
       - test_django_stop_to_motor_service : send_command("stop") -> process_command -> status idle
       - test_django_status_read_after_command : verifier que get_motor_status retourne un JSON valide apres chaque commande
       - test_encoder_read_through_client : verifier que get_encoder_status retourne les donnees encodeur

    Pattern : utiliser patch sur les paths IPC dans les DEUX cotes (ipc_manager + django settings.MOTOR_SERVICE_IPC) pour pointer vers les memes fichiers tmp_path.

    Eviter : ne PAS lancer la boucle run() du MotorService — appeler process_command directement (meme pattern que test_integration_ipc.py)
  </action>
  <verify>uv run pytest tests/test_cross_layer.py -v — tous les tests passent</verify>
  <done>AC-1 et AC-2 satisfaits : flux cross-couche GOTO, JOG, STOP valide de bout en bout</done>
</task>

<task type="auto">
  <name>Task 2: Validation suite complete</name>
  <files>tests/</files>
  <action>
    Executer la suite complete et verifier :
    1. `uv run pytest -v` — 738+ tests, 0 echecs
    2. Si des tests echouent, corriger les tests (pas le code prod) en alignant sur l'API reelle
    3. Verifier que les nouveaux tests cross-layer ne cassent rien
  </action>
  <verify>uv run pytest -v — 0 failed, 0 errors</verify>
  <done>AC-3 satisfait : suite complete verte</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- core/* (code production stable depuis Phase 3-4)
- services/* (code production stable depuis Phase 4)
- web/* (vues API stables depuis Phase 5)
- tests existants (738 tests — ne pas modifier)

## SCOPE LIMITS
- Pas de correction du bug ObjectListView.get_objets_disponibles() (issue differee)
- Pas d'implementation des routes park/calibrate/end-session (issue differee)
- Pas de tests de performance ou de charge
- Pas de tests necessitant du hardware reel (GPIO, encodeur)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_cross_layer.py -v` — tous les tests passent
- [ ] `uv run pytest -v` — suite complete 738+ tests, 0 echecs, 0 erreurs
- [ ] Aucun fichier production modifie
</verification>

<success_criteria>
- Tests cross-couche valident le flux Django -> IPC -> MotorService
- Suite complete verte (738+ tests + nouveaux)
- Zero regression
</success_criteria>

<output>
After completion, create `.paul/phases/06-validation/06-01-SUMMARY.md`
</output>
